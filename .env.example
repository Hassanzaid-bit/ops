# Neon Postgres connection string (from Neon dashboard → Connect)
# Add this to .env.local (Next.js) or .env — both work for db:push / db:seed
DATABASE_URL=postgresql://neondb_owner:npg_UQwrA8zL5hFN@ep-summer-sun-axvc5rq8-pooler.c-4.us-east-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# Used to sign the httpOnly session JWT (openssl rand -hex 32)
SESSION_SECRET=90b94a626fb5bc02e04506ea146d9a67f723468abc5c0395b4b3b9f873a5f4d9

# Stabilizes Server Action IDs across deploys (openssl rand -base64 32)
NEXT_SERVER_ACTIONS_ENCRYPTION_KEY=
